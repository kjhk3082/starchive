# OS / editor
.DS_Store
*.swp

# Rust (Cargo.lock IS committed — this is a binary app, for reproducible builds)
/target

# Local user data — generated at runtime, never part of the tool repo
*.db
*.db-wal
*.db-shm
/data
/archive

# Env
.env
